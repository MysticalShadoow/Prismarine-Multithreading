import { Entity } from "prismarine-entity";
import { Physics } from "../physics/engines/physics";
import { EntityState } from "../physics/states/entityState";
export declare class StateStorage {
    private _internal;
    constructor();
    get length(): number;
    get latestTick(): number;
    get oldestTick(): number;
    get(tick: number): EntityState;
    getOldest(): EntityState;
    getLatest(): EntityState;
    getPrevious(): void;
    push(tick: number, entity: Entity, ctx: Physics): this;
    pushRaw(tick: number, entityState: EntityState): this;
    removeOldest(): this;
    removeNewest(): this;
}
