/// <reference types="node" />
import { Vec3 } from "vec3";
import { Bot } from "mineflayer";
import EventEmitter from "events";
import { TestState } from "./constants";
import { BaseGoal } from "./goals";
export declare class JumpPathing extends EventEmitter {
    private bot;
    searchDepth: number;
    goal: BaseGoal | null;
    nextLandingPosition?: Vec3;
    plannedJumps: Vec3[];
    private potentialLandingPositions;
    constructor(bot: Bot);
    goto(goal: BaseGoal): void;
    stop(): void;
    private perTick;
    resetInfo(): void;
    resetEverything(): void;
    performJumps: () => Promise<void>;
    simulateJump(playerState: any, searchDepth: number, parentJumpPos?: TestState): void;
}
